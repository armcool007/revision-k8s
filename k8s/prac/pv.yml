apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-pod
spec:
  capacity:
    storage: 200Mi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: ""
  hostPath:
    path: /opt/mysql
