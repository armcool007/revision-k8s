apiVersion: v1
kind: ReplicationController
metadata:
  name: rcapp
spec:
  replicas: 2
  selector:
    app: javaapp
  template:
    metadata:
      name: testcase
      labels:
        app: javaapp
    spec:
        containers:
          - name: container
            image: armcool004/color_splash_from_cursorai
            ports:
              - containerPort: 80


