apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  name: app-pod 
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: app-pod
  minReplicas: 1
  maxReplicas: 10
  targetCPUUtilizationPercentage: 20

