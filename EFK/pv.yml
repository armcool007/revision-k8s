apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-pod
  namespace: efk
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: sc-testing
  persistentVolumeReclaimPolicy: Retain
  capacity:
    storage: 200Mi
  hostPath:
    path: /tmp
